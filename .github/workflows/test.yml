name: Simple Workflow Dispatch

on:
  workflow_dispatch:

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.ref == 'refs/heads/main' && 'main' || 'test_workflow_dispatch' || 'branch_2' || 'branch_3 '}}

      - name: Run a script
        run: echo "This is a manually triggered workflow on the 'diferent' branch!"
