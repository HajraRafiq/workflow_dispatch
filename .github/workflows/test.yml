name: Simple Workflow Dispatch

on:
  workflow_dispatch:

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.ref == 'refs/heads/main' && 'main' || 'branch_2' }}

      - name: Run a script
        run: echo "This is a manually triggered workflow on the 'main' branch!"
